/* 
Arquivo contendo alguns exemplos de variáveis, 
com seus tipos e nomes.
-----
char: um byte que armazena o código de um caractere do conjunto de caracteres local, também pode armazenar números inteiros.
int: um número inteiro
float: um número real com precisão simples
double: um número real com precisão dupla
char[12]: um texto com tamanho de 12 caracteres
-----
A declaração de uma variável segue o seguinte padrão:
    <tipo> <nome> 
Exemplos: 
int numero;

*/
#include <stdio.h> // incluindo uma biblioteca padrão
// Variáveis
int main(void){ // declaração função
    // declarando que uma variável do tipo int
    int idade; // 1, 4, 2, 5, 6, 3
    // declarando que uma variável do tipo float
    float nota; // 10.2,33.2, 12.21111
    char nome;
    short float_;
    long a; //long altura;
    double h;
    // 1nome
    // palavras reservadas
    // nome não-semânticos
    return 0;
}